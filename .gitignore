.env
node_modules/
picaudit/

# Bot-approved runtime gallery additions should live on the server.
# Existing seeded gallery files are already tracked; see docs/GALLERY_DATA.md
# for the one-time server command that keeps photos.json from conflicting.
public/gallery/images/img_*
